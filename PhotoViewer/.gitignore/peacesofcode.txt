'''
#button functionalities
funcObj = Dir('directory_Object')
def getInformation():
    funcObj.getPath()
    funcObj.getItems()
def placeImage(isImage=False):
    if not isImage:
        image = funcObj.getImage()
        imageP = ImageTk.PhotoImage(image)
        label = tk.Label(frame, image = imageP)
        label.photo = imageP
        label.pack()
        isImage=True
    else:
        label.photo = None
        isImage=False
def openDirectory():
    getInformation()
    placeImage()
def Pindex():
    funcObj.getPindex()
    placeImage()
def Mindex():
    funcObj.getMindex()
    placeImage()'''





    def getPindex(self):
        self.index+=1
        if self.index>self.items-1:
            self.index=0

    def getMindex(self):
        self.index-=1
        if self.index<(-self.items):
            self.index=0
